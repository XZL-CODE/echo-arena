# Repository Instructions

本文件保存通用协作约定，项目专属的长期规则由用户按需补充。

## 需求范围

- 围绕用户目标和已确认需求完成任务，遵循项目现有约定；必要的实现细节可自主决定。
- 需求未明确、且会改变功能范围、业务规则或权限的选择，先说明关键差异和影响并确认；不得将推测或额外改进直接作为已确认需求。
- 交付前核对结果与任务目标是否一致，及时修正无关扩展。

## 文档阅读

- 按当前任务阶段和待解决问题读取相关 Agent Note 与 README；只有现有信息不足以支持判断时才扩大阅读范围。信息足以推进任务后，及时进入讨论、设计或实施。无需一口气读完所有过往文档，初步只阅读核心内容（相关 Agent Note、README.md）。`相关材料/` 的读取按下节规则。

## 文件读写范围

- 始终读取：`AGENTS.md`、`.agents/notes/README.md`。两者只由人维护，Agent 不自行修改。
- 改动 `AGENTS.md`、Notes 规范、Note 的 `Proposal` 段，或用户要求先看的内容时，不动文件：先在回复里以 diff 列出改动，等用户回复后再改，改后不再贴 diff。
- diff 的写法：`--- 文件` `+++ 文件`，`@@` 内写所在章节标题，`-` 行逐字取自文件当前原文（先读取），`+` 行是改后原文；整段搬动而未改的内容用一行说明，不逐行列出；新建文件贴全文。
- 按任务读取和维护：代码、测试、各模块 `README.md`、`.agents/notes/**`。README 只写当前说明，按主题组织，不按日期追加。
- `相关材料/**` 是人维护的输入与归档。只有用户消息直接指定了文件，或任务明确要求核对某份 Note 链接的来源时才读取；在仓库内搜索时排除该目录。Agent 不在其中新建、修改或删除任何文件。
- 仓库内不新建上述之外的 md、记录目录或会话导出。任务过程、测试计数、未执行事项写在回复中。
- 不可逆的外部操作（数据库写入、发送、部署）在对应 Note 的 Verification 或提交说明中记一行时间与范围。

## 代码可读性与验证

- 遵循项目已有的格式化配置；新增或修改的人工维护代码应保持结构清晰，不制造压缩式长行，也不顺手格式化与当前任务无关的区域。
- 触及已有压缩代码时，只整理完成本次修改所需的最小完整区块；大范围格式化作为独立任务处理。
- 完成修改后，运行与变更范围相匹配的格式、语法和测试检查。

## Agent Notes

- 简单更新按需同步受影响的 README.md。
- 涉及需要长期保留的决策、方向或关键取舍时，先完整阅读 [Notes 规范](.agents/notes/README.md)，搜索相关 Note，并在同一次变更中按规范新增或更新。Note 简记决定、理由和关键边界。
- 实现无法按字面满足相关 Note `Proposal` 段中某一条时，不改该条、不绕过：按 Notes 规范另建 proposed Note 并在该条标待裁定，等用户裁定。
- 存在与 Note 明确相关的输入材料（需求表、会议纪要、第三方契约）时，在 Note 中链接其入口；归档材料由材料目录的 README 说明，不从 Note 链接。
